#!/usr/bin/env groovy

pipelineJob('shipping-deploy-jenkins-operator') {
    displayName('shipping-deploy')

    logRotator {
        numToKeep(30)
        daysToKeep(90)
    }

    configure { project ->
        project / 'properties' / 'org.jenkinsci.plugins.workflow.job.properties.DurabilityHintJobProperty' {
            hint('PERFORMANCE_OPTIMIZED')
        }
    }

    definition {
        cpsScm {
            scm {
                git {
                    remote {
                        url('https://github.com/cloud-architecture-demo/sock-shop-configuration.git')
                    }
                    branches('*/demo-sf2')
                }
            }
            scriptPath('jenkins-seed-jobs/socks-shop/shipping/deploy/jobs/shipping_deploy_pipeline.jenkins')
        }
    }
}
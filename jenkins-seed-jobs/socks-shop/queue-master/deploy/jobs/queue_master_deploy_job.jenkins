#!/usr/bin/env groovy

pipelineJob('queue-master-deploy-jenkins-operator') {
    displayName('queue-master-deploy')

    logRotator {
        numToKeep(30)
        daysToKeep(90)
    }

    configure { project ->
        project / 'properties' / 'org.jenkinsci.plugins.workflow.job.properties.DurabilityHintJobProperty' {
            hint('PERFORMANCE_OPTIMIZED')
        }
    }

    definition {
        cpsScm {
            scm {
                git {
                    remote {
                        url('https://github.com/cloud-architecture-demo/sock-shop-configuration.git')
                    }
                    branches('*/demo-sf2')
                }
            }
            scriptPath('jenkins-seed-jobs/socks-shop/queue-master/deploy/jobs/queue_master_deploy_pipeline.jenkins')
        }
    }
}
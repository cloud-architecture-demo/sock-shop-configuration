#!/usr/bin/env groovy

pipelineJob('orders-deploy-jenkins-operator') {
    displayName('orders-deploy')

    logRotator {
        numToKeep(30)
        daysToKeep(90)
    }

    configure { project ->
        project / 'properties' / 'org.jenkinsci.plugins.workflow.job.properties.DurabilityHintJobProperty' {
            hint('PERFORMANCE_OPTIMIZED')
        }
    }

    definition {
        cpsScm {
            scm {
                git {
                    remote {
                        url('https://github.com/cloud-architecture-demo/sock-shop-configuration.git')
                    }
                    branches('*/gke-jcasc-demo')
                }
            }
            scriptPath('jenkins-seed-jobs/socks-shop/orders/deploy/jobs/orders_deploy_pipeline.jenkins')
        }
    }
}